const nome = prompt("Qual seu primeiro nome?")
const endereco = prompt("Qual sueu endereço?")
const idade = prompt('qual sua idade?')

alert(`
    ${nome}, voce tem ${idade} anos,\n mora no Endereço ${Endereço}
    `)
